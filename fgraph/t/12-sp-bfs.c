#include <tap.h>
#include "fgraph.h"

int main() {
    tap_plan(0);
    
    /* directed + weighted */
    
    /* undirected + weighted */
    
    /* directed + unweighted */
    
    /* undirected + unweighted */
    
    /* no path */
    
    return tap_status();
}
